import { ForgotPasswordDetails } from './forgot-password-details';

describe('ForgotPasswordDetails', () => {
  it('should create an instance', () => {
    expect(new ForgotPasswordDetails()).toBeTruthy();
  });
});
