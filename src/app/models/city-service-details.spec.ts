import { CityServiceDetails } from './city-service-details';

describe('CityServiceDetails', () => {
  it('should create an instance', () => {
    expect(new CityServiceDetails()).toBeTruthy();
  });
});
